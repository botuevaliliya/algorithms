#Даны 2 строки A и B. Обе строки имеют одинаковую длину N. Для каждой позиции 1<=i<=N строки В, нужно вычислить тип совпадения в этой позиции со
#строкой А. Если Bi=Ai, то в позиции i тип совпадения должен быть равен P. Если Bi!=Ai, но существует другая позиция 1<=j<=N, такая что Bi=Aj, 
#то в позиции i тип совпадения должен быть равен S. Буквы в рамках одной строки могут повторяться. Каждую букву строки А можно использовать не 
#более чем в одном совпадении типа P и S. Предпочтение всегда отдается типу P. В случае совпадения типа S, предпочтение всегда отдается самой левой 
#позиции в строке А. В остальных позициях тип совпадения должен быть равен I. Выведите единственную строку С, где Сi - тип совпадения символа Bi.



 a = input()
 b = input()
 anb = [i for i in set(b) if i in set(a)]
 dk = {i:0 for i in anb}
 for i in range(len(a)):
     if a[i]==b[i]:
         print('P', end='')
     elif b[i] in anb:
         f = 0
         for j in range(dk[b[i]], len(a)):
             if a[j]==b[i] and a[j]!=b[j]:
                 f = 1
                 break
         if f == 1:
             print('S', end='')
             dk[b[i]] = j + 1
         else:
             print('I', end='')
     else:
         print('I', end='')
 print()
